<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yu.mapper.ExerciseRecordMapper">

    <insert id="insertRecord">
        insert into exercise_record(user_openid, exercise_date, running_route, distance, time)
        values (#{userOpenid},#{exerciseDate},#{runningRoute},#{distance},#{time})
    </insert>
</mapper>
